# p2p-copy
p2p-copy is a Python-based command-line tool designed to transfer large files (10MB to 100MB+) between Linux systems where neither party can open inbound ports. It acts as a secure bridge, bypassing NAT restrictions by using intermediate temporary storage or a custom relay server.
